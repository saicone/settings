apply plugin: 'maven-publish'
apply plugin: 'signing'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = project.group
            artifactId = project.name
            version = project.version

            pom {
                afterEvaluate {
                    name = project.description
                    description = project.description
                }
                url = 'https://github.com/saicone/settings'

                organization {
                    name = 'saicone'
                    url = 'https://github.com/saicone'
                }

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/license/MIT'
                        distribution = 'repo'
                    }
                }

                developers {
                    developer {
                        id = 'Rubenicos'
                        name = 'Rubén Chavarría'
                        email = 'ruben.chavarria@saicone.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/saicone/settings.git'
                    developerConnection = 'scm:git:ssh://github.com/saicone/settings.git'
                    url = 'https://github.com/saicone/settings'
                }

                issueManagement {
                    system = 'GitHub'
                    url = 'https://github.com/saicone/settings/issues'
                }
            }
        }
    }

    repositories {
        maven {
            name = 'Deployment'

            url = version.endsWith("SNAPSHOT") ?
                    uri("https://central.sonatype.com/repository/maven-snapshots/") :
                    uri("https://ossrh-staging-api.central.sonatype.com/service/local/staging/deploy/maven2/")

            credentials {
                username = project.findProperty("maven.publish.username") ?: ""
                password = project.findProperty("maven.publish.password") ?: ""
            }
        }
    }
}

signing {
    useGpgCmd()
    sign publishing.publications.maven
}